name: Go

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.22"

      - name: Golangci-lint
        uses: golangci/golangci-lint-action@v6.1.1

      # - name: Install Formatter gofumpt
      #   run: go install mvdan.cc/gofumpt@latest

      # - name: Run Formatter gofumpt
      #   run: gofumpt -l -w .

      - name: Run Build
        run: go build -v ./...

      - name: Run Tests
        run: go test -v ./...
